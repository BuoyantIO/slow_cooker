version: 2
jobs:
  build:
    docker:
    - image: cimg/go:1.21.4
    steps:
    - checkout
    - restore_cache:
        keys:
          - go-build-cache
    - run: go test -v ./...
    - save_cache:
        key: go-build-cache
        paths:
          - ~/.cache/go-build
